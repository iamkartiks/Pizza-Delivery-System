version: '3'

services:
  # db:
  #   image: mysql:latest
  #   environment:
  #     MYSQL_ROOT_PASSWORD: "@Arianna22"
  #     MYSQL_DATABASE: pizzeria_db
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - ./mysql-data:/var/lib/mysql

  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["python", "manage.py", "runserver", "0.0.0.0:8000"] # "db", "./wait-for-db.sh", 
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    # depends_on:
    #   - db
